# Runtime dependencies, fully pinned — a rebuild must not change the stack.
# Resolved against Python 3.11 (see runtime.txt); Render installs this file.
#
# Top-level deps are Flask, Flask-SocketIO, eventlet, gunicorn, openpyxl.
# python-socketio/python-engineio are transitive but pinned explicitly: they
# speak the wire protocol that static/js/socket.io.min.js is frozen against.
#
# To regenerate after changing a top-level pin:
#   python3.11 -m venv /tmp/pinvenv
#   /tmp/pinvenv/bin/pip install <the top-level deps, pinned>
#   /tmp/pinvenv/bin/pip freeze >> requirements.txt   (keep this header)
#
# Test-only deps live in requirements-dev.txt.
bidict==0.24.1
blinker==1.9.0
click==8.5.0
dnspython==2.8.0
et_xmlfile==2.0.0
eventlet==0.41.0
Flask==3.1.3
Flask-SocketIO==5.6.1
greenlet==3.5.5
gunicorn==22.0.0
h11==0.16.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
openpyxl==3.1.5
packaging==26.3
python-engineio==4.13.3
python-socketio==5.16.3
simple-websocket==1.1.0
typing_extensions==4.16.0
Werkzeug==3.1.8
wsproto==1.3.2
